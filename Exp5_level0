#include<stdio.h>
int main()
{
    char a[20];
    scanf("%s",a);
    int i=0;
    while(a[i]!='\0')
    {
        if(a[i]>=65 && a[i]<=91)
        {
           a[i]=a[i]+32;
        }
        i++;
    }
    printf("%s",a);
    return 0;
} 
