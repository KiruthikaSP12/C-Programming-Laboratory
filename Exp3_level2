#include<stdio.h>
int main()
{
    int n,row,col;
    scanf("%d",&n);
    for(row=n;row>=1;row--)
    {
        for(col=row;col>=1;col--)
        {
            printf("%d\t",col);
        }
        printf("\n");
    }
       return 0;
}
